;; Theme:

(defvar toaster-theme 'doom-dracula
    "A symbol representing the Emacs theme to load at startup.
  Set to `default' to load no theme at all. Defaults to the excellent 'doom-one' theme")

(menu-bar-mode -1)

(toggle-scroll-bar -1)

(tool-bar-mode -1)
