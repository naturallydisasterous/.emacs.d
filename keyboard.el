;;writeroom mode:

;;(global-set-key (kbd "C-x w") 'writeroom-mode) ;;Ctrl+X w

;; vterm

;;(global-set-key (kbd "C-x t") 'vterm) ;; Ctrl-x t

(nvmap :keymaps 'override :prefix "SPC"
  ;;"c c"   '(counsel-M-x :which-key "M-x")
)
